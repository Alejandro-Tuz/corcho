# Base de datos
DATABASE_URL=postgresql+psycopg://corcho:corcho_dev@localhost:5433/corcho

# Redis
REDIS_URL=redis://localhost:6379/0

# Aplicación
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:5173

# URL pública para generar el QR de la sala
PUBLIC_BASE_URL=http://localhost:5173

# Resumen con IA (Anthropic). Vacío = la función queda deshabilitada sin romper
# el resto de la app (core/config.py: ai_api_key es el único campo de Settings
# sin obligar, a propósito -ver el diseño del resumen con IA).
AI_API_KEY=